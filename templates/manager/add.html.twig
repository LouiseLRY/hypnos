{% extends 'base.html.twig' %}

{% block title %}Hypnos Management - Ajouter une suite{% endblock %}

{% block content %}

    <div class="heading-page text-center">
        <h2>Bienvenue {{ app.user.firstname }}, <br> Vous gérez l'hôtel "{{ app.user.establishment.name }}"</h2>
    </div>

    <div class="container frame">
        <div class="row">
            <div class="col">
                <h3>Ajouter une suite - {{ app.user.establishment }}</h3>
                {{ form(form) }}
            </div>
        </div>
    </div>

{% endblock %}
